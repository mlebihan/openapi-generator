openapi: 3.0.1
info:
  version: 1.0.0
  title: requestBody of application/object-stream type
  license:
    name: MIT
servers:
  - url: http://api.example.xyz/v1
paths:
  /test1:
    post:
      requestBody:
        required: true
        content:
          application/octet-stream:
              schema:
                type: object

      responses:
        "200":
          content:
            application/json:
              schema:
                type: object

          description: successful
